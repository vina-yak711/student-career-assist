services:
  - type: web
    name: vynkai-careerforge-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 4000
      - key: NODE_ENV
        value: production
