{
  "name": "student-career-assist-root",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js",
    "server": "node server/index.js",
    "client": "npm run dev --prefix client"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  }
}